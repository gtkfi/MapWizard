<UserControl x:Class="MapWizard.View.TractAggregationResultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ScrollViewer Margin="5,30,20,10"  HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"  HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <TextBlock DataContext="{Binding Source={StaticResource Locator}, Path=TractAggregationViewModel}" Name="SummaryPDF" Text="{Binding Result.TractAggregationSummary}"/>
        </ScrollViewer>
    </Grid>
</UserControl>
